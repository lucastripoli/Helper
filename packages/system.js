$( document ).ready(function() {
    //name
    //description

    var obj = [];

    name = "system";

    obj.description = "Configurations and action in this program";

    obj = function(var0){
    }

    addToActualObject(name, obj);
    system.load();
});
